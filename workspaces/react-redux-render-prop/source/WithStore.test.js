import React from 'react'
// import { render } from 'react-testing-library'
// import { WithStore } from './'

describe('WithStore', () => {
  it('renders store props through provider', () => {
    // const TestWithStore = WithStore.Provider((value = { testValue: 1 }))
    // const { container } = render(<TestWithStore />)
    expect(true).toBe(true)
  })
})
